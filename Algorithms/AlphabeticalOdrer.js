function order(str){
	for (var x=0; var x<str.length; x++){
		if str[x+1] <= str[x]{
			return false;
		}
	}
	return true;
}

//The following funciton takes in a string of letters as a parameter
//and checks to see if the string is in alphabetical order. 